# Task

Replace the active response target with a same-business-day target. Append, rather than rewrite, the requirement history:

- FACT-001: Add `REQ-001@2` as `active` with `Respond to each request on the same business day.` Update the task-board and evidence-plan documents under `docs/` so each references `REQ-001@2` only.
- FACT-002: Retain `REQ-001@1` with its exact 24-hour target, changing only its state to `superseded`.

The synthetic requirement history is append-only.
