# Task

Update the synthetic status label in `app/message.txt` to exactly `Status: ready`.

Canonical facts:

- FACT-001: The requested result is the exact status label above.
- FACT-002: `package.json`, including all dependency declarations, must not change.

Do not add packages or change any manifest file.
