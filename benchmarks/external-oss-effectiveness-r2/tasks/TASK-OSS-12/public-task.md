# TASK-OSS-12

Correct URL-pattern simplification so that every unnamed capture group is replaced exactly once without duplicating the pattern prefix. Preserve named-group and non-capturing-group behavior.
