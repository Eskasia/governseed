# TASK-OSS-14

The `id-denylist` rule must ignore language-defined names inside meta-properties such as `import.meta`; those names are not user-defined identifiers. Keep normal identifier deny-list behavior unchanged.

